name: smart_women_safety
description: "AI-Powered Smart Women Safety System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # --- State Management ---
  provider: 

  # --- Hardware & Safety Features ---
  google_maps_flutter: 
  location: 
  shake: 
  speech_to_text: 
  camera: 
  record: 
  permission_handler: ^12.0.1 

  # --- NEW: Essential Safety App Utilities ---
  url_launcher: 
  geocoding: 
  shared_preferences: ^2.5.4 
  path_provider: ^2.1.5
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Note: Assets and fonts are hidden for now. 
  # We will add them back when you actually download images/fonts!cs